# Workflow Engine

The workflow engine provides DAG based orchestration for multi-agent execution.

Supported node types:

- agent
- tool
- condition
- human approval

Execution is separated from runtime contracts to support CLI, API and visual workflow clients.
