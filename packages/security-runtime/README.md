# Security Runtime

Security boundary for Agent execution.

Responsibilities:

- Agent capability permissions
- Tool execution policy
- Resource access control
- Future sandbox integration

The runtime is designed as a policy layer between Agent Runtime and external execution resources.
